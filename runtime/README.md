# runtime/

The userspace strand runtime library: light threads, scheduler-activation
handling, introspection (docs/CONCURRENCY.md). Arrives with BUILD-ORDER.md
step 7 (two-level scheduler + strands).
