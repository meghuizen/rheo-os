# services/

System service cells: the state store, the host reconciler, controllers,
exporters (docs/CONTAINERS-KUBERNETES.md, docs/OBSERVABILITY.md). These are
userspace cells, not kernel code. Arrives with BUILD-ORDER.md phase 5.
