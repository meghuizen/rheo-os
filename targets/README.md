# targets/

Custom target JSON specs, only for cases the built-in Rust targets cannot
express (docs/DEVELOPMENT.md 2). Currently empty: x86_64-unknown-none,
aarch64-unknown-none-softfloat and riscv64gc-unknown-none-elf cover all three ISAs.
